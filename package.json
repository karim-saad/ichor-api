{
    "scripts": {
        "build": "sam build",
        "deploy": "npm run build && sam deploy",
        "invoke": "sam local invoke --profile ichor",
        "lint": "pylint",
        "start-api": "sam local start-api --profile ichor"
    }
}
